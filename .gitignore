.DS_Store
node_modules/

# User's global MCP config (not the plugin's .mcp.json which IS versioned)
# Plugin root .mcp.json ships HTTP-only endpoints for Cowork connector discovery
# per Anthropic's official knowledge-work-plugins pattern (Feb 2026)

# Insights reports (archived to ~/.claude/insights/)
*.html
The AI Context Vault.pdf

# Session telemetry logs
.claude/logs/

# Adversarial review and evaluation artifacts (session-local, not versioned)
.claude/agent-evaluations/
.claude/reviews/

# Desktop Code worktree checkouts (local session artifacts)
.claude/worktrees/

# CCC execution loop state files (runtime, not versioned)
.ccc-state.json
.ccc-progress.md
.ccc-circuit-breaker.json
.ccc-agent-teams.json
.ccc-agent-teams-log.jsonl
.ccc-preferences.yaml

# Editor/tool-specific settings (not versioned)
.claude/settings.json
.continues-handoff.md
.cursor/
